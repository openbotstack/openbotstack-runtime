version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign

  exclusions:
    rules:
      - path: '(.+)_test\.go'
        linters:
          - errcheck
